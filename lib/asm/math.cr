# Will contain simple functions to generate assembly for math operations

module ASM
  # generates code for adding r1 and r2 together
  def add(r1 : Register, r2 : Register) : String
    # TODO
  end
end
