class Scanner
    def initialize
        
    end

    # scan() takes the input, an array of bytes, and breaks it into tokens.
    # throws an UnscannableException if the input cannot be successfully scanned.
    # or something like that
    def scan(input: Array(Byte))

    end
end
